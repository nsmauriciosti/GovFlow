version: '3.8'

services:
  govflow-app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        - API_KEY=${API_KEY}
    container_name: govflow_pro_app
    ports:
      - "3000:3000"
    environment:
      - API_KEY=${API_KEY}
    restart: always
